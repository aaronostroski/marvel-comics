export type GlobalStyleTypes = {
  html: {
    padding: number;
    margin: number;
    height: string;
    width: string;
    overflow: string;
    "box-sizing": string;
  };
  body: {
    padding: number;
    margin: number;
    height: string;
    width: string;
    overflow: string;
    "box-sizing": string;
  };
};
